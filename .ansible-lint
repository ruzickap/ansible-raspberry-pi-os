---
warn_list:
  - internal-error
skip_list:
  # Role name oefenweb.fail2ban does not match ``^[a-z][a-z0-9_]+$`` pattern
  - 106
  # Lines should be no longer than 160 chars
  - 204
  # Package installs should not use latest
  - 403

verbosity: 1
